function showToast(message,type='success',duration=3200){const root=document.getElementById('toast-root');if(!root)return;const icons={success:'check-circle',error:'times-circle',warning:'triangle-exclamation',info:'circle-info'};const t=document.createElement('div');t.className=`toast toast-${type}`;t.innerHTML=`<i class="fa-solid fa-${icons[type]||icons.info}"></i><span></span>`;t.querySelector('span').textContent=message;root.append(t);setTimeout(()=>{t.style.opacity='0';t.style.transform='translateX(120%)';setTimeout(()=>t.remove(),240)},duration)}
async function postJSON(url,data){const f=new FormData();Object.entries(data).forEach(([k,v])=>f.append(k,v));f.append('csrf_token',document.querySelector('meta[name="csrf-token"]')?.content||'');const r=await fetch(url,{method:'POST',body:f});return r.json()}
